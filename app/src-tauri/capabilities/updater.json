{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater-window",
  "description": "The updates window: it listens for the shell's `update` events and calls the shell's own update_* commands. The panel's window is served by the daemon and has no IPC at all.",
  "windows": [
    "updater"
  ],
  "permissions": [
    "core:default"
  ]
}
